# The launch board intentionally uses Markdown two-space hard line breaks.
# Several long-lived fail-closed PR workflows compare against their historical
# baseline, so current-main board-only formatting must not masquerade as PR
# whitespace damage. Scope the exception to this authority file only.
eksamio-learning-engine/00B-PROJECT-PRIORITIES-CURRENT.md -whitespace
