<script>
(function(){
"use strict";
var ROOT_ID="ege-physics-demo-2026", STORAGE_KEY="eksamio_ege_physics_demo_2026_v2", DURATION_MS=235*60*1000;
function byId(id){return document.getElementById(id)}
function parseData(id){var e=byId(id);if(!e)throw new Error("Не найден блок данных "+id);return JSON.parse(e.textContent)}
function escapeHtml(v){return String(v==null?"":v).replace(/[&<>"']/g,function(c){return{"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]})}
function freshState(){return{version:2,status:"idle",current:1,answers:{},drafts:{},flagged:{},startedAt:null,endsAt:null,completedAt:null,variant26:null,selfScores:{},shortScore:0}}
function loadState(){try{var raw=localStorage.getItem(STORAGE_KEY);if(!raw)return freshState();var parsed=JSON.parse(raw);return parsed&&parsed.version===2?Object.assign(freshState(),parsed):freshState()}catch(e){return freshState()}}
function showStorageError(){var el=byId("ephys-storage-warning");if(el)el.setAttribute("data-show","true")}
function saveState(){try{localStorage.setItem(STORAGE_KEY,JSON.stringify(state));return true}catch(e){showStorageError();return false}}
function dataAll(){var tasks=[];["ephys-data-1","ephys-data-2","ephys-data-3"].forEach(function(id){tasks=tasks.concat(parseData(id).tasks||[])});tasks.sort(function(a,b){return a.number-b.number});return{tasks:tasks,variants:parseData("ephys-data-4").variants26||[]}}
var root=byId(ROOT_ID), data=dataAll(), state=loadState(), timerHandle=null, lastModalFocus=null;
function ensureVariant(){if(!state.variant26||state.variant26<1||state.variant26>3)state.variant26=1+Math.floor(Math.random()*3)}
function task26(){ensureVariant();return data.variants.find(function(v){return v.variant===state.variant26})||data.variants[0]}
function allTasks(){return data.tasks.concat([task26()])}
function getTask(n){return n===26?task26():data.tasks.find(function(t){return t.number===n})}
function getAnswer(n){return state.answers[n]}
function isFilled(t,v){if(t.kind==="measurement")return !!(v&&String(v.value||"").trim()&&String(v.error||"").trim());return String(v||"").trim().length>0}
function canonicalMinus(s){return String(s||"").replace(/[−–—]/g,"-")}
function strictNumberString(value){var s=canonicalMinus(value).trim().replace(",", ".");if(!/^-?(?:0|[1-9]\d*)(?:\.\d+)?$/.test(s))return null;return s}
function strictDigits(value,len){var s=String(value||"");return new RegExp("^\\d{"+len+"}$").test(s)?s:null}
function strictUniqueDigits(value){var s=String(value||"");if(!/^\d+$/.test(s))return null;return new Set(s).size===s.length?s:null}
function unorderedScore(t,value){var raw=strictUniqueDigits(value);if(raw===null)return 0;var C=new Set(t.answer.split("")),G=new Set(raw.split(""));var missing=0,extra=0;C.forEach(function(x){if(!G.has(x))missing++});G.forEach(function(x){if(!C.has(x))extra++});if(missing===0&&extra===0)return t.maxScore;if(t.maxScore===1)return 0;if((missing===1&&extra===0)||(missing===0&&extra===1)||(missing===1&&extra===1&&G.size===C.size))return 1;return 0}
function scoreTask(t,v){
 if(t.kind==="extended"||t.kind==="extended26")return 0;
 if(t.kind==="numeric"){var a=strictNumberString(v),b=strictNumberString(t.answer);return a!==null&&b!==null&&a===b?t.maxScore:0}
 if(t.kind==="ordered"){var r=strictDigits(v,t.answer.length);if(r===null)return 0;if(r===t.answer)return t.maxScore;var d=0;for(var i=0;i<r.length;i++)if(r[i]!==t.answer[i])d++;return d===1?1:0}
 if(t.kind==="unordered_multi"||t.kind==="unordered_exact")return unorderedScore(t,v);
 if(t.kind==="measurement"){return v&&strictNumberString(v.value)==="136"&&strictNumberString(v.error)==="3"?1:0}
 return 0
}
function isValidForm(t,v){if(!isFilled(t,v))return true;if(t.kind==="numeric")return strictNumberString(v)!==null;if(t.kind==="ordered")return strictDigits(v,t.answer.length)!==null;if(t.kind==="unordered_multi"||t.kind==="unordered_exact")return strictUniqueDigits(v)!==null;if(t.kind==="measurement")return strictNumberString(v.value)!==null&&strictNumberString(v.error)!==null;return true}
function userAnswer(t,v){if(t.kind==="measurement")return v?(String(v.value||"")+" ± "+String(v.error||"")):"—";return String(v||"—")}
function formatTime(ms){var x=Math.max(0,Math.floor(ms/1000)),h=Math.floor(x/3600),m=Math.floor(x%3600/60),s=x%60;return[h,m,s].map(function(n){return String(n).padStart(2,"0")}).join(":")}
function updateRootState(){root.setAttribute("data-state",state.status==="running"?"running":state.status==="finished"?"finished":"idle")}
function updateStartControls(){byId("ephys-start").classList.toggle("ephys-hidden",state.status!=="idle");byId("ephys-resume").classList.toggle("ephys-hidden",state.status!=="running");byId("ephys-reset-start").classList.toggle("ephys-hidden",state.status==="idle")}
function filledCount(){return allTasks().filter(function(t){return isFilled(t,getAnswer(t.number))}).length}
function buildNav(){var n=byId("ephys-nav");n.innerHTML="";for(var i=1;i<=26;i++){(function(num){var b=document.createElement("button");b.type="button";b.className="ephys-nav-btn";b.textContent=num;b.setAttribute("aria-label","Открыть задание "+num);b.addEventListener("click",function(){state.current=num;saveState();renderTask()});n.appendChild(b)})(i)}}
function updateNav(){var buttons=byId("ephys-nav").querySelectorAll("button");buttons.forEach(function(b,i){var n=i+1,t=getTask(n);b.setAttribute("aria-current",n===state.current?"true":"false");b.setAttribute("data-filled",isFilled(t,getAnswer(n))?"true":"false");b.setAttribute("data-flagged",state.flagged[n]?"true":"false")});byId("ephys-progress-bar").style.width=Math.round(filledCount()/26*100)+"%"}
function focusables(el){return Array.from(el.querySelectorAll('button,[href],input,select,textarea,[tabindex]:not([tabindex="-1"])')).filter(function(x){return !x.disabled&&!x.hidden})}
function openModal(title,body){lastModalFocus=document.activeElement;byId("ephys-modal-title").textContent=title;byId("ephys-modal-body").innerHTML=body;var modal=byId("ephys-modal"),card=modal.querySelector(".ephys-modal__card");modal.setAttribute("data-open","true");modal.setAttribute("aria-hidden","false");card.focus()}
function closeModal(){var modal=byId("ephys-modal");modal.removeAttribute("data-open");modal.setAttribute("aria-hidden","true");if(lastModalFocus&&document.contains(lastModalFocus))lastModalFocus.focus()}
function answerField(t){var v=getAnswer(t.number);if(t.kind==="measurement"){v=v||{value:"",error:""};return '<div class="ephys-answer"><div class="ephys-pair"><div><label for="ephys-value">Показание манометра</label><input class="ephys-input" id="ephys-value" inputmode="decimal" value="'+escapeHtml(v.value)+'"></div><div><label for="ephys-error">Абсолютная погрешность</label><input class="ephys-input" id="ephys-error" inputmode="decimal" value="'+escapeHtml(v.error)+'"></div></div><p class="ephys-help">'+escapeHtml(t.answerHelp||"")+'</p><p class="ephys-validation" id="ephys-validation"></p><p class="ephys-save-status" id="ephys-save-status">'+(isFilled(t,v)?"✓ Ответ сохранён автоматически":"Ответ будет сохранён автоматически при вводе")+'</p></div>'}
 if(t.kind==="extended"||t.kind==="extended26")return '<div class="ephys-answer"><label for="ephys-answer-input">Полное решение</label><textarea class="ephys-textarea" id="ephys-answer-input" spellcheck="true">'+escapeHtml(v||"")+'</textarea><p class="ephys-help">Запишите обоснование, законы, преобразования, вычисления и ответ.</p><p class="ephys-save-status" id="ephys-save-status">'+(isFilled(t,v)?"✓ Ответ сохранён автоматически":"Ответ будет сохранён автоматически при вводе")+'</p></div>';
 var mode=t.number===1?"text":(t.kind==="numeric"?"decimal":"numeric");return '<div class="ephys-answer"><label for="ephys-answer-input">Ответ</label><input class="ephys-input" id="ephys-answer-input" inputmode="'+mode+'" autocomplete="off" spellcheck="false" value="'+escapeHtml(v||"")+'"><p class="ephys-help">'+escapeHtml(t.answerHelp||"Запишите ответ в указанной форме.")+'</p><p class="ephys-validation" id="ephys-validation"></p><p class="ephys-save-status" id="ephys-save-status">'+(isFilled(t,v)?"✓ Ответ сохранён автоматически":"Ответ будет сохранён автоматически при вводе")+'</p></div>'}
function draftField(t){return '<details class="ephys-draft"><summary><strong>Черновик</strong> — не участвует в проверке</summary><textarea class="ephys-textarea" id="ephys-draft-input">'+escapeHtml(state.drafts[t.number]||"")+'</textarea></details>'}
function updateValidation(t,v){var valid=isValidForm(t,v),el=byId("ephys-validation");if(el)el.textContent=valid?"":"Используйте только требуемые цифры или число без пробелов, букв и дополнительных знаков.";var inputs=root.querySelectorAll("#ephys-answer-input,#ephys-value,#ephys-error");inputs.forEach(function(i){i.setAttribute("aria-invalid",valid?"false":"true")});return valid}
function saveStatus(ok,filled){var el=byId("ephys-save-status");if(el)el.textContent=ok?(filled?"✓ Ответ сохранён автоматически":"Ответ будет сохранён автоматически при вводе"):"Не удалось сохранить ответ в браузере"}
function bindZoom(c){c.querySelectorAll(".ephys-zoom").forEach(function(b){b.addEventListener("click",function(){var f=b.closest(".ephys-figure");openModal("Схема к заданию",f.querySelector(".ephys-figure__canvas").innerHTML)})})}
function renderTask(){var t=getTask(state.current),stage=byId("ephys-task-stage");stage.innerHTML='<div class="ephys-task-head"><div><div class="ephys-task-number">Задание '+t.number+' из 26'+(t.number===26?' · вариант '+state.variant26:'')+'</div><h2 class="ep-section-title">'+escapeHtml(t.title)+'</h2></div><span class="ep-pill">'+t.maxScore+' '+(t.maxScore===1?'балл':'балла')+'</span></div><div class="ephys-task-prompt">'+t.promptHtml+'</div>'+answerField(t)+draftField(t)+'<div class="ephys-task-actions"><div class="ephys-task-actions__group"><button class="ep-button ep-button--small ep-button--secondary" id="ephys-prev" type="button"'+(t.number===1?' disabled':'')+'>Назад</button><button class="ep-button ep-button--small" id="ephys-next" type="button">'+(t.number===26?'Завершить экзамен':'Перейти к следующему заданию')+'</button></div><div class="ephys-task-actions__group"><button class="ep-button ep-button--small ep-button--secondary" id="ephys-flag" type="button">'+(state.flagged[t.number]?'Снять отметку':'Вернуться к заданию позже')+'</button></div></div>';
 if(t.kind==="measurement"){var a=byId("ephys-value"),e=byId("ephys-error");function savePair(){state.answers[t.number]={value:a.value,error:e.value};var ok=saveState();updateValidation(t,state.answers[t.number]);saveStatus(ok,isFilled(t,state.answers[t.number]));updateNav()}a.addEventListener("input",savePair);e.addEventListener("input",savePair)}else{byId("ephys-answer-input").addEventListener("input",function(ev){state.answers[t.number]=ev.target.value;var ok=saveState();updateValidation(t,ev.target.value);saveStatus(ok,isFilled(t,ev.target.value));updateNav()})}
 byId("ephys-draft-input").addEventListener("input",function(ev){state.drafts[t.number]=ev.target.value;saveState()});byId("ephys-prev").addEventListener("click",function(){if(t.number>1){state.current=t.number-1;saveState();renderTask()}});byId("ephys-next").addEventListener("click",function(){if(t.number<26){state.current=t.number+1;saveState();renderTask()}else finishExam(false)});byId("ephys-flag").addEventListener("click",function(){state.flagged[t.number]=!state.flagged[t.number];saveState();renderTask()});bindZoom(stage);updateNav();updateValidation(t,getAnswer(t.number));window.scrollTo({top:Math.max(0,root.getBoundingClientRect().top+window.scrollY-15),behavior:"smooth"})}
function startExam(){state=freshState();ensureVariant();var now=Date.now();state.status="running";state.startedAt=now;state.endsAt=now+DURATION_MS;saveState();updateRootState();updateStartControls();buildNav();renderTask();startTimer()}
function resumeExam(){ensureVariant();if(!state.endsAt||state.endsAt<=Date.now()){finishExam(true);return}updateRootState();updateStartControls();buildNav();renderTask();startTimer()}
function startTimer(){clearInterval(timerHandle);function tick(){var left=(state.endsAt||0)-Date.now();byId("ephys-timer").textContent=formatTime(left);if(left<=0){clearInterval(timerHandle);finishExam(true)}}tick();timerHandle=setInterval(tick,1000)}
function calculateShortScore(){return allTasks().filter(function(t){return t.number<=20}).reduce(function(s,t){return s+scoreTask(t,getAnswer(t.number))},0)}
function finishExam(auto){var empty=allTasks().filter(function(t){return !isFilled(t,getAnswer(t.number))}).length,flagged=Object.keys(state.flagged).filter(function(k){return state.flagged[k]}).length;if(!auto&&!confirm("Без ответа: "+empty+". Отмечено вернуться позже: "+flagged+". После завершения ответы нельзя будет изменить. Завершить экзамен?"))return;clearInterval(timerHandle);state.status="finished";state.completedAt=Date.now();state.shortScore=calculateShortScore();saveState();updateRootState();updateStartControls();renderResults();window.scrollTo({top:Math.max(0,root.getBoundingClientRect().top+window.scrollY-15),behavior:"smooth"})}
function selfDefinitions(){return[{key:"21",label:"Задание 21",max:3},{key:"22",label:"Задание 22",max:2},{key:"23",label:"Задание 23",max:2},{key:"24",label:"Задание 24",max:3},{key:"25",label:"Задание 25",max:3},{key:"26k1",label:"Задание 26 · К1 — обоснование",max:1},{key:"26k2",label:"Задание 26 · К2 — решение",max:3}]}
function extendedComplete(){return selfDefinitions().every(function(d){return state.selfScores[d.key]!==undefined&&state.selfScores[d.key]!==null&&state.selfScores[d.key]!==""})}
function extendedScore(){return selfDefinitions().reduce(function(s,d){return s+Number(state.selfScores[d.key]||0)},0)}
function buildSelfAssessment(){var w=byId("ephys-self-grid");w.innerHTML="";selfDefinitions().forEach(function(d){var opts='<option value="">Не оценено</option>';for(var i=0;i<=d.max;i++)opts+='<option value="'+i+'"'+(String(state.selfScores[d.key])===String(i)?' selected':'')+'>'+i+' из '+d.max+'</option>';var r=document.createElement("div");r.className="ephys-self-row";r.innerHTML='<label><strong>'+d.label+'</strong><br><span class="ephys-mini">Максимум '+d.max+'</span></label><select>'+opts+'</select>';r.querySelector("select").addEventListener("change",function(ev){state.selfScores[d.key]=ev.target.value===""?null:Number(ev.target.value);saveState();renderScoreCards()});w.appendChild(r)})}
function renderScoreCards(){byId("ephys-short-score").textContent=state.shortScore;var ok=extendedComplete(),e=extendedScore();byId("ephys-extended-score").textContent=ok?e:"—";byId("ephys-total-score").textContent=ok?state.shortScore+e:"—";byId("ephys-result-note").textContent=ok?"Все развёрнутые задания оценены. Общий первичный балл рассчитан.":"Оцените все задания 21–26, чтобы получить общий первичный балл."}
function renderReview(){var w=byId("ephys-review");w.innerHTML="";allTasks().forEach(function(t){var item=document.createElement("article");item.className="ephys-review-item";if(t.number<=20){var pts=scoreTask(t,getAnswer(t.number));item.setAttribute("data-correct",pts===t.maxScore?"true":"false");item.innerHTML='<h3>Задание '+t.number+' · '+pts+'/'+t.maxScore+'</h3><p>Ваш ответ: <strong>'+escapeHtml(userAnswer(t,getAnswer(t.number)))+'</strong></p><p>Ответ ФИПИ: <strong>'+escapeHtml(t.answerDisplay)+'</strong></p><p class="ephys-mini">Для этой позиции в демоверсии опубликован эталон краткого ответа, но не отдельное подробное решение.</p>'}else if(t.number<26){item.innerHTML='<h3>Задание '+t.number+' · максимум '+t.maxScore+'</h3><p><strong>Ваше решение</strong></p><div class="ephys-solution">'+escapeHtml(getAnswer(t.number)||"Ответ не введён").replace(/\n/g,"<br>")+'</div><details open><summary><strong>Возможное решение ФИПИ</strong></summary><div class="ephys-solution">'+t.solutionHtml+'</div><p class="ephys-mini">Приведённое ФИПИ возможное решение не является единственным корректным способом.</p></details><details><summary><strong>Критерии оценивания ФИПИ</strong></summary><div class="ephys-criteria">'+t.criteriaHtml+'</div></details>'}else{item.innerHTML='<h3>Задание 26 · вариант '+state.variant26+' · максимум 4</h3><p><strong>Ваше решение</strong></p><div class="ephys-solution">'+escapeHtml(getAnswer(26)||"Ответ не введён").replace(/\n/g,"<br>")+'</div><details open><summary><strong>Возможное решение ФИПИ</strong></summary><div class="ephys-solution">'+t.solutionHtml+'</div><p class="ephys-mini">Приведённое ФИПИ возможное решение не является единственным корректным способом.</p></details><details><summary><strong>Критерий К1 ФИПИ — обоснование</strong></summary><div class="ephys-criteria">'+t.criteriaK1Html+'</div></details><details><summary><strong>Критерий К2 ФИПИ — решение</strong></summary><div class="ephys-criteria">'+t.criteriaK2Html+'</div></details>'}w.appendChild(item);bindZoom(item)})}
function renderResults(){buildSelfAssessment();renderScoreCards();renderReview()}
function resetAttempt(){if(!confirm("Удалить текущую попытку и начать заново?"))return;try{localStorage.removeItem(STORAGE_KEY)}catch(e){}state=freshState();updateRootState();updateStartControls();window.scrollTo({top:Math.max(0,root.getBoundingClientRect().top+window.scrollY-15),behavior:"smooth"})}
byId("ephys-start").addEventListener("click",startExam);byId("ephys-resume").addEventListener("click",resumeExam);byId("ephys-reset-start").addEventListener("click",resetAttempt);byId("ephys-reset-result").addEventListener("click",resetAttempt);byId("ephys-finish-top").addEventListener("click",function(){finishExam(false)});byId("ephys-reference").addEventListener("click",function(){openModal("Справочные данные",parseData("ephys-reference-data").html)});byId("ephys-modal-close").addEventListener("click",closeModal);byId("ephys-modal").addEventListener("click",function(ev){if(ev.target===byId("ephys-modal"))closeModal()});document.addEventListener("keydown",function(ev){var modal=byId("ephys-modal");if(ev.key==="Escape")closeModal();if(ev.key==="Tab"&&modal.getAttribute("data-open")==="true"){var f=focusables(modal);if(!f.length)return;var first=f[0],last=f[f.length-1];if(ev.shiftKey&&document.activeElement===first){ev.preventDefault();last.focus()}else if(!ev.shiftKey&&document.activeElement===last){ev.preventDefault();first.focus()}}});
ensureVariant();saveState();updateRootState();updateStartControls();if(state.status==="finished")renderResults();else if(state.status==="running"){if(!state.endsAt||state.endsAt<=Date.now())finishExam(true);else resumeExam()}
})();
</script>