name: Apply one-shot base mathematics task 6 table order fix

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/math-base-row-table-order-fix.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python ege-matematika-baza-demoversiya-2026/scripts/fix_row_table_order.py
      - run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm ege-matematika-baza-demoversiya-2026/scripts/fix_row_table_order.py .github/workflows/math-base-row-table-order-fix.yml
          git add ege-matematika-baza-demoversiya-2026/templates/runtime.js
          git commit -m 'Render task 6 interactive table in official source order'
          git push origin HEAD:main
