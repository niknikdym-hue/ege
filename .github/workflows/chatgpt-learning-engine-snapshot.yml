name: ChatGPT Learning Engine Snapshot
on:
  push:
    branches:
      - chatgpt-learning-engine-validation-snapshot-20260812
jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Upload Learning Engine snapshot
        uses: actions/upload-artifact@v4
        with:
          name: eksamio-learning-engine-main-snapshot
          path: eksamio-learning-engine/
          if-no-files-found: error
          compression-level: 6
