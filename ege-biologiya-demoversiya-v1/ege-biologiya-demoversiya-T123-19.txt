<script>
(function(){'use strict';
const ROOT=document.getElementById('eksamio-bio-demo');if(!ROOT)return;
const DATA=JSON.parse((window.__BIO_TASK_CHUNKS__||[]).join(''));
const TASKS=DATA.tasks,ASSETS=JSON.parse((window.__BIO_ASSET_CHUNKS__||[]).join(''));
const STORAGE_KEY=DATA.storage_key||'eksamio_ege_biologiya_demo_2026_v1_0_2';
const $=(s,c=ROOT)=>c.querySelector(s),$$=(s,c=ROOT)=>Array.from(c.querySelectorAll(s));
const safe={get(){try{return JSON.parse(localStorage.getItem(STORAGE_KEY)||'null')}catch(e){return null}},set(v){try{localStorage.setItem(STORAGE_KEY,JSON.stringify(v));return true}catch(e){return false}},remove(){try{localStorage.removeItem(STORAGE_KEY)}catch(e){}}};
const group={};TASKS.forEach(t=>(group[t.number]||(group[t.number]=[])).push(t));
function blank(){return{version:DATA.package_version,started:false,finished:false,current:1,endsAt:0,answers:{},extendedText:{},flags:[],variants:{},self:{}}}
let loaded=safe.get(),state=loaded&&loaded.version===DATA.package_version?Object.assign(blank(),loaded):blank(),timerId=null;
function randomInt(n){try{const a=new Uint32Array(1);crypto.getRandomValues(a);return a[0]%n}catch(e){return Math.floor(Math.random()*n)}}
function chooseVariants(forced){if(!state.variants['4'])state.variants['4']=String((forced&&forced['4'])||1+randomInt(2));if(!state.variants['56'])state.variants['56']=String((forced&&forced['56'])||1+randomInt(2));if(!state.variants['24'])state.variants['24']=String((forced&&forced['24'])||1+randomInt(2));if(!state.variants['27'])state.variants['27']=String((forced&&forced['27'])||1+randomInt(4));}
function selected(n){let v='1';if(n===4)v=state.variants['4'];else if(n===5||n===6)v=state.variants['56'];else if(n===24)v=state.variants['24'];else if(n===27)v=state.variants['27'];return group[n].find(x=>x.variant_id===String(v))||group[n][0]}
function selectedTasks(){return Array.from({length:28},(_,i)=>selected(i+1))}
function save(){state.version=DATA.package_version;safe.set(state)}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function normRaw(v){return String(v??'')}
function scoreShort(t,input){const raw=normRaw(input),canon=t.answer.canonical,acc=t.answer.accepted_values||[canon];if(!raw||raw.includes('_'))return 0;if(t.scoring==='exact_word'){if(!/^[А-Яа-яЁё]+$/u.test(raw))return 0}else if(t.scoring==='accepted_exact'){if(!/^\d+(?:,\d+)?$/.test(raw))return 0}else if(!/^\d+$/.test(raw))return 0;switch(t.scoring){case'exact_word':return raw.toLowerCase().replace(/ё/g,'е')===canon.toLowerCase().replace(/ё/g,'е')?t.max_score:0;case'exact_digits':case'accepted_exact':return acc.includes(raw)?t.max_score:0;case'positional_one_error':if(raw.length!==canon.length)return 0;let d=0;for(let i=0;i<canon.length;i++)if(raw[i]!==canon[i])d++;return d===0?2:d===1?1:0;case'unordered_one_error':{const a=[...raw],b=[...canon];if(new Set(a).size!==a.length)return 0;if(a.length===b.length&&a.slice().sort().join('')===b.slice().sort().join(''))return 2;const count=(arr,x)=>arr.filter(y=>y===x).length;const symbols=new Set([...a,...b]);let missing=0,extra=0;symbols.forEach(x=>{const da=count(b,x)-count(a,x);if(da>0)missing+=da;else extra-=da});if((missing===1&&extra===0)||(missing===0&&extra===1)||(missing===1&&extra===1&&a.length===b.length))return 1;return 0}case'sequence_two_errors':{const compare=raw.length>canon.length?raw.slice(0,canon.length):raw;let d=Math.abs(compare.length-canon.length);for(let i=0;i<Math.min(compare.length,canon.length);i++)if(compare[i]!==canon[i])d++;let base=d===0?2:d<=2?1:0;if(raw.length>canon.length)base=Math.max(0,base-1);return base}default:return 0}}
function structuredComplete(t,value){const it=t.interaction||{},v=String(value||'');if(['table_selects','matching_selects'].includes(it.type))return new RegExp('^[1-9]{'+it.answer_length+'}$').test(v);if(it.type==='sequence_builder')return v.length===it.answer_length&&/^\d+$/.test(v)&&new Set(v).size===v.length;if(it.type==='multiple_choice')return /^\d+$/.test(v)&&new Set(v).size===v.length;return Boolean(v)}
function scoreState(t,value){const type=t.interaction&&t.interaction.type;if(['table_selects','matching_selects','sequence_builder'].includes(type)&&!structuredComplete(t,value))return 0;return scoreShort(t,value)}
function evalRule(t,vals){const ids=t.criterion_elements.map(e=>e.id),checked=ids.filter(id=>vals[id]);for(const r of t.score_rules){let ok=false;if(r.type==='all')ok=checked.length===ids.length;else if(r.type==='count')ok=checked.length>=r.min;else if(r.type==='count_required')ok=checked.length>=r.min&&(r.required||[]).every(x=>vals[x]);else if(r.type==='count_or_required')ok=checked.length>=r.min||(r.required_sets||[]).some(s=>s.every(x=>vals[x]));else if(r.type==='count_required_or_set')ok=(checked.length>=r.min&&(r.required||[]).every(x=>vals[x]))||(r.required_sets||[]).some(s=>s.every(x=>vals[x]));if(ok)return r.score}return 0}
function isAnswered(n){const t=selected(n);if(t.kind==='extended')return Boolean((state.extendedText[n]||'').trim());return structuredComplete(t,state.answers[n]||'')}
function bindAssets(ctx){$$('[data-bio-asset]',ctx).forEach(img=>{const id=img.dataset.bioAsset;if(ASSETS[id])img.src=ASSETS[id];img.tabIndex=0;img.title='Нажмите, чтобы увеличить';img.addEventListener('click',()=>openImage(img));img.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();openImage(img)}})})}
function openImage(img){$('#bio-modal-body').innerHTML='';const cp=img.cloneNode();cp.removeAttribute('data-bio-asset');cp.removeAttribute('title');$('#bio-modal-body').appendChild(cp);$('#bio-image-modal').classList.add('is-open');$('#bio-image-modal').setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}
function closeImage(){$('#bio-image-modal').classList.remove('is-open');$('#bio-image-modal').setAttribute('aria-hidden','true');document.body.style.overflow=''}
function promptHtml(t){if(!(t.interaction&&t.interaction.hide_prompt_options))return t.prompt_html;const box=document.createElement('div');box.innerHTML=t.prompt_html;box.querySelectorAll('.bio-options').forEach(x=>x.remove());return box.innerHTML}
function codeBox(value,complete,message='заполните все позиции'){const shown=value?value.replace(/_/g,'—'):'—';return `<div class="bio-code-box ${complete?'':'is-incomplete'}" aria-live="polite"><span>Ответ для бланка:</span><strong>${esc(shown)}</strong>${complete?'':`<span class="bio-hint">${esc(message)}</span>`}</div>`}
function selectField(t){const it=t.interaction,value=String(state.answers[t.number]||'').padEnd(it.answer_length,'_').slice(0,it.answer_length);const used=new Set([...value].filter(x=>x!=='_'));const rows=it.rows.map((r,i)=>{const current=value[i]==='_'?'':value[i];const options=['<option value="">Выберите</option>'].concat(it.choices.map(o=>`<option value="${o.value}" ${current===o.value?'selected':''} ${!it.allow_repeat&&used.has(o.value)&&current!==o.value?'disabled':''}>${o.value}) ${esc(o.text)}</option>`)).join('');return `<div class="bio-control-row"><label class="bio-control-label" for="bio-select-${t.number}-${i}"><b>${esc(r.key)})</b> ${esc(r.text)}</label><select class="bio-select" id="bio-select-${t.number}-${i}" data-structured-select="${i}">${options}</select></div>`}).join('');return `<div class="bio-structured-answer"><fieldset><legend>${it.type==='matching_selects'?'Установите соответствие':'Заполните таблицу'}</legend><div class="bio-control-grid">${rows}</div>${codeBox(value.replace(/_+$/,''),structuredComplete(t,value))}</fieldset></div>`}
function multipleField(t){const it=t.interaction,selectedSet=new Set(String(state.answers[t.number]||'').split('').filter(Boolean)),atLimit=it.max_select&&selectedSet.size>=it.max_select;const opts=it.options.map(o=>`<label class="bio-option-control"><input type="checkbox" data-multi-value="${o.value}" ${selectedSet.has(o.value)?'checked':''} ${atLimit&&!selectedSet.has(o.value)?'disabled':''}><span><span class="bio-option-number">${o.value})</span> ${esc(it.source_sentences?'Предложение '+o.value:o.text)}</span></label>`).join('');const note=it.max_select?`Выбрано: ${selectedSet.size} из ${it.max_select}.`:`Выбрано: ${selectedSet.size}. Количество верных утверждений в условии не указано.`;return `<div class="bio-structured-answer"><fieldset><legend>Выберите варианты ответа</legend><div class="bio-option-controls">${opts}</div><div class="bio-selection-note ${it.max_select&&selectedSet.size!==it.max_select?'is-warning':''}">${note}</div>${codeBox([...selectedSet].sort().join(''),selectedSet.size>0,'выберите хотя бы один вариант')}</fieldset></div>`}
function sequenceField(t){const it=t.interaction,value=String(state.answers[t.number]||''),selectedSet=new Set(value.split('').filter(Boolean));const chips=value?value.split('').map(x=>`<span class="bio-sequence-chip">${x}</span>`).join(''):'<span class="bio-sequence-empty">Нажимайте элементы в правильном порядке</span>';const opts=it.options.map(o=>`<button type="button" class="bio-sequence-button" data-sequence-value="${o.value}" ${selectedSet.has(o.value)?'disabled':''}><b>${o.value}</b><span>${esc(o.text)}</span></button>`).join('');return `<div class="bio-structured-answer"><fieldset><legend>Соберите последовательность</legend><div class="bio-sequence-current" aria-live="polite">${chips}</div><div class="bio-sequence-options">${opts}</div><div class="bio-structured-actions"><button type="button" class="bio-inline-btn" id="bio-sequence-undo" ${value?'':'disabled'}>Отменить последний выбор</button><button type="button" class="bio-inline-btn" id="bio-sequence-reset" ${value?'':'disabled'}>Очистить последовательность</button></div>${codeBox(value,structuredComplete(t,value))}</fieldset></div>`}
function answerField(t){if(t.kind==='extended')return `<div class="bio-answer-box"><label class="bio-label" for="bio-ext">Развёрнутый ответ</label><textarea class="bio-textarea" id="bio-ext" placeholder="Запишите ответ так, как записывали бы его в бланке № 2.">${esc(state.extendedText[t.number]||'')}</textarea><div class="bio-hint">Критерии и модель ответа откроются только после завершения попытки.</div></div>`;const type=t.interaction&&t.interaction.type;if(type==='text_input')return `<div class="bio-answer-box"><label class="bio-label" for="bio-answer">Ответ</label><input class="bio-input" id="bio-answer" autocomplete="off" inputmode="text" value="${esc(state.answers[t.number]||'')}"><div class="bio-hint">${esc(t.input_hint||'')}</div></div>`;if(type==='table_selects'||type==='matching_selects')return selectField(t);if(type==='sequence_builder')return sequenceField(t);if(type==='multiple_choice')return multipleField(t);return ''}
function showStart(){$('#bio-app').classList.remove('is-active');$('#bio-results').classList.remove('is-active');$('#bio-start').style.display='block';const active=state.started&&!state.finished;$('#bio-continue-btn').hidden=!active;$('#bio-new-btn').hidden=!active;$('#bio-start-btn').hidden=active}
function start(forced){state=blank();state.started=true;state.endsAt=Date.now()+235*60*1000;chooseVariants(forced);save();showApp();render();startTimer()}
function showApp(){$('#bio-start').style.display='none';$('#bio-results').classList.remove('is-active');$('#bio-app').classList.add('is-active')}
function tick(){if(!state.started||state.finished)return;const left=Math.max(0,state.endsAt-Date.now()),h=Math.floor(left/3600000),m=Math.floor(left%3600000/60000),s=Math.floor(left%60000/1000);$('#bio-timer').textContent=[h,m,s].map(x=>String(x).padStart(2,'0')).join(':');if(left<=0)finish(true)}
function startTimer(){clearInterval(timerId);tick();timerId=setInterval(tick,1000)}
function renderNav(){const nav=$('#bio-nav');nav.innerHTML='';for(let n=1;n<=28;n++){const b=document.createElement('button');b.type='button';b.className='bio-nav-btn';b.textContent=n;b.setAttribute('aria-label',`Задание ${n}${isAnswered(n)?', ответ сохранён':''}${state.flags.includes(n)?', отмечено':''}`);if(isAnswered(n))b.classList.add('is-answered');if(state.flags.includes(n))b.classList.add('is-flagged');if(n===state.current)b.classList.add('is-current');b.onclick=()=>{state.current=n;save();render();window.scrollTo({top:0,behavior:'smooth'})};nav.appendChild(b)}}
function bindAnswerControls(t){const inp=$('#bio-answer');if(inp)inp.oninput=e=>{state.answers[t.number]=e.target.value;save();renderNav()};const ta=$('#bio-ext');if(ta)ta.oninput=e=>{state.extendedText[t.number]=e.target.value;save();renderNav()};$$('[data-structured-select]').forEach(sel=>sel.onchange=()=>{const it=t.interaction,chars=String(state.answers[t.number]||'').padEnd(it.answer_length,'_').slice(0,it.answer_length).split('');chars[Number(sel.dataset.structuredSelect)]=sel.value||'_';state.answers[t.number]=chars.join('');save();render()});$$('[data-multi-value]').forEach(cb=>cb.onchange=()=>{const it=t.interaction,set=new Set(String(state.answers[t.number]||'').split('').filter(Boolean));if(cb.checked){if(!it.max_select||set.size<it.max_select)set.add(cb.dataset.multiValue)}else set.delete(cb.dataset.multiValue);state.answers[t.number]=[...set].sort((a,b)=>Number(a)-Number(b)).join('');save();render()});$$('[data-sequence-value]').forEach(btn=>btn.onclick=()=>{const it=t.interaction,v=String(state.answers[t.number]||''),x=btn.dataset.sequenceValue;if(!v.includes(x)&&v.length<it.answer_length){state.answers[t.number]=v+x;save();render()}});const undo=$('#bio-sequence-undo');if(undo)undo.onclick=()=>{state.answers[t.number]=String(state.answers[t.number]||'').slice(0,-1);save();render()};const reset=$('#bio-sequence-reset');if(reset)reset.onclick=()=>{delete state.answers[t.number];save();render()}}
function render(){if(state.finished){renderResults();return}const t=selected(state.current);$('#bio-progress').textContent=`Задание ${t.number} из 28`;$('#bio-progress-sub').textContent=t.kind==='short'?`Часть 1 · максимум ${t.max_score} ${t.max_score===1?'балл':'балла'}`:'Часть 2 · максимум 3 балла';renderNav();const variant=t.variant_label?`<span class="bio-variant">${esc(t.variant_label)}</span>`:'';const q=$('#bio-question');q.innerHTML=`<div class="bio-question-head"><div><div class="bio-question-number">Задание ${t.number}</div>${variant}</div><div class="bio-question-meta">Источник: ФИПИ 2026<br>печатная страница ${t.source.prompt_printed_page}</div></div><div class="bio-prompt">${promptHtml(t)}</div>${answerField(t)}<div class="bio-question-actions"><div class="bio-action-group"><button class="bio-btn bio-btn-ghost" id="bio-prev" ${t.number===1?'disabled':''}>Предыдущее</button><button class="bio-btn bio-btn-primary" id="bio-next" ${t.number===28?'disabled':''}>Следующее</button></div><div class="bio-action-group"><button class="bio-btn bio-btn-ghost" id="bio-flag">${state.flags.includes(t.number)?'Снять отметку':'Отметить'}</button><button class="bio-btn bio-btn-danger" id="bio-clear">Сбросить ответ</button></div></div>`;bindAssets(q);bindAnswerControls(t);$('#bio-prev').onclick=()=>go(-1);$('#bio-next').onclick=()=>go(1);$('#bio-flag').onclick=toggleFlag;$('#bio-clear').onclick=clearCurrent}
function go(d){state.current=Math.max(1,Math.min(28,state.current+d));save();render();window.scrollTo({top:0,behavior:'smooth'})}
function toggleFlag(){const n=state.current;state.flags=state.flags.includes(n)?state.flags.filter(x=>x!==n):[...state.flags,n];save();render()}
function clearCurrent(){const t=selected(state.current);if(t.kind==='short')delete state.answers[t.number];else{delete state.extendedText[t.number];delete state.self[t.number]}save();render()}
function shortTotal(){return selectedTasks().filter(t=>t.kind==='short').reduce((s,t)=>s+scoreState(t,state.answers[t.number]||''),0)}
function extendedScore(t){if(!(state.extendedText[t.number]||'').trim())return 0;const vals=state.self[t.number]||{};const base=evalRule(t,vals);return Math.max(0,base-(vals.__irrelevantError?1:0))}
function selfTotal(){return selectedTasks().filter(t=>t.kind==='extended').reduce((s,t)=>s+extendedScore(t),0)}
function finish(skip){if(!skip&&!confirm('Завершить попытку и открыть результаты?'))return;state.finished=true;clearInterval(timerId);save();renderResults()}
function answerLabel(t){const v=String(state.answers[t.number]||'');if(!v)return 'Ответ не дан';if(['table_selects','matching_selects','sequence_builder'].includes(t.interaction.type)&&!structuredComplete(t,v))return 'Ответ не завершён';return v.replace(/_/g,'—')}
function renderResults(){const sh=shortTotal(),ex=selfTotal();$('#bio-app').classList.remove('is-active');$('#bio-start').style.display='none';const box=$('#bio-results');box.classList.add('is-active');let review='';selectedTasks().forEach(t=>{if(t.kind==='short'){const user=answerLabel(t),score=scoreState(t,state.answers[t.number]||''),cls=score===t.max_score?'is-ok':score>0?'is-part':'is-bad';review+=`<article class="bio-review-card ${cls}"><div class="bio-review-title">Задание ${t.number}${t.variant_label?' · '+esc(t.variant_label):''}: ${score}/${t.max_score}</div><div class="bio-user-answer">Ваш ответ: ${esc(user)}</div><div class="bio-model"><strong>Ответ ФИПИ:</strong> ${esc((t.answer.accepted_values||[t.answer.canonical]).join(' или '))}</div></article>`}else{const vals=state.self[t.number]||{},hasAnswer=Boolean((state.extendedText[t.number]||'').trim()),score=extendedScore(t),disabled=hasAnswer?'':'disabled',checks=t.criterion_elements.map(e=>`<label class="bio-check ${hasAnswer?'':'is-disabled'}"><input type="checkbox" data-self-task="${t.number}" data-self-id="${e.id}" ${vals[e.id]?'checked':''} ${disabled}><span>${esc(e.text)}</span></label>`).join(''),penalty=t.self_assessment&&t.self_assessment.irrelevant_error_penalty?`<label class="bio-check bio-penalty ${hasAnswer?'':'is-disabled'}"><input type="checkbox" data-self-penalty="${t.number}" ${vals.__irrelevantError?'checked':''} ${disabled}><span>В дополнительной информации, не относящейся к вопросу, есть биологическая ошибка: вычесть 1 балл.</span></label>`:'';review+=`<article class="bio-review-card"><div class="bio-review-title">Задание ${t.number}${t.variant_label?' · '+esc(t.variant_label):''}: учебная самооценка <span id="bio-self-score-${t.number}">${score}</span>/3</div><div class="bio-user-answer">Ваш ответ: ${esc(state.extendedText[t.number]||'Ответ не введён')}</div><div class="bio-model">${t.model_answer_html}${t.notes_html||''}</div><div class="bio-self"><strong>${hasAnswer?'Отметьте элементы, которые есть в вашем ответе':'Самооценка недоступна: развёрнутый ответ не введён'}</strong>${checks}${penalty}<div class="bio-self-score">Самооценка: <span data-self-display="${t.number}">${score}</span>/3</div><div class="bio-hint">Это учебная самооценка по критериям ФИПИ, а не официальная проверка экспертом.</div></div></article>`}});box.innerHTML=`<h2>Результаты попытки</h2><div class="bio-score-grid"><div class="bio-score"><b>${sh}/36</b><span>автоматически проверенная первая часть</span></div><div class="bio-score is-unofficial"><b id="bio-self-total">${ex}/21</b><span>учебная самооценка второй части</span></div><div class="bio-score is-official-pending"><b>—/57</b><span>официальный первичный балл определяется после экспертной проверки</span></div></div><div class="bio-result-note"><strong>Важно:</strong> баллы за задания 22–28 выставляют эксперты. Учебная самооценка второй части не прибавляется к результату первой части и не образует официальный общий балл.</div><div class="bio-start-actions"><button class="bio-btn bio-btn-primary" id="bio-retry">Новая попытка</button></div><div class="bio-review">${review}</div>`;bindAssets(box);$$('[data-self-task]',box).forEach(c=>c.onchange=()=>{const n=Number(c.dataset.selfTask);state.self[n]=state.self[n]||{};state.self[n][c.dataset.selfId]=c.checked;save();updateSelf(n,box)});$$('[data-self-penalty]',box).forEach(c=>c.onchange=()=>{const n=Number(c.dataset.selfPenalty);state.self[n]=state.self[n]||{};state.self[n].__irrelevantError=c.checked;save();updateSelf(n,box)});$('#bio-retry').onclick=()=>{safe.remove();state=blank();showStart();window.scrollTo({top:0,behavior:'smooth'})};window.scrollTo({top:0,behavior:'smooth'})}
function updateSelf(n,box){const t=selected(n),v=extendedScore(t);$$(`[data-self-display="${n}"]`,box).forEach(x=>x.textContent=v);const label=$(`#bio-self-score-${n}`,box);if(label)label.textContent=v;$('#bio-self-total').textContent=`${selfTotal()}/21`}
$('#bio-start-btn').onclick=()=>start(window.__BIO_TEST_VARIANTS__||null);$('#bio-continue-btn').onclick=()=>{showApp();render();startTimer()};$('#bio-new-btn').onclick=()=>{if(confirm('Удалить сохранённую попытку и начать заново?'))start(window.__BIO_TEST_VARIANTS__||null)};$('#bio-finish-top').onclick=()=>finish(false);$('#bio-modal-close').onclick=closeImage;$('#bio-image-modal').onclick=e=>{if(e.target.id==='bio-image-modal')closeImage()};document.addEventListener('keydown',e=>{if(e.key==='Escape')closeImage()});
window.BiologyDemoTestApi={scoreShort:(n,v,input)=>{const t=(group[n]||[]).find(x=>x.variant_id===String(v))||group[n][0];return scoreShort(t,input)},scoreState:(n,input)=>scoreState(selected(n),input),startWithVariants:v=>start(v),getState:()=>JSON.parse(JSON.stringify(state)),getSelectedTask:n=>selected(n),getAllTasks:()=>TASKS,setAnswer:(n,v)=>{state.answers[n]=String(v);save();if(state.current===n)render()},setExtended:(n,v)=>{state.extendedText[n]=String(v);save()},goTo:n=>{state.current=Number(n);save();render()},isAnswered,finishNow:()=>finish(true),shortTotal,selfTotal,extendedScore:n=>extendedScore(selected(n)),clear:()=>{safe.remove();state=blank();showStart()}};
chooseVariants();save();if(state.started){if(state.finished)renderResults();else showStart()}else showStart();
})();
</script>