EKSAMIO BASE MATH 2024 - INTERACTION CONTRACT
Status: LOCKED BEFORE BUILD
Date: 2026-08-13

General:
- 21 positions, one assigned official example per position.
- Assigned official example number is visible, persisted on reload, and used in scoring/results.
- All short-answer scoring is automatic, max 21.
- Format validation is separate from correctness.
- Raw invalid input is preserved.

Controls:
1 numeric input
2 four real selects (A,B,V,G -> option number)
3 numeric input
4 numeric input
5 probability numeric input, range 0..1
6 v1 real checkboxes translators; v2 real checkboxes suitcases; v3 numeric input
7 four real selects for every variant
8 real checkboxes for statements; scorer compares selected set, order irrelevant
9 numeric input
10 numeric input
11 numeric input
12 numeric input
13 numeric input
14 numeric input
15 numeric input
16 numeric input
17 numeric input
18 four real selects for every variant
19 numeric input with multiple official alternatives per variant
20 numeric input
21 numeric input

Special scorer semantics:
6.1 valid selected sets are {1,3,5} or {2,5,6}. If a raw code representation is supported, every FIPI permutation remains valid; checkbox UI stores/scorers as a set.
6.2 valid selected set is {1,5}; raw 15/51 are equivalent only if raw-code compatibility exists.
6.3 official answer 192000.
8.1 valid set {2,4}; 24/42 are equivalent representations.
8.2 valid set {1,4}; 14/41 are equivalent representations.
19.1 valid numerical answers: 578,587,758,785,857,875.
19.2 valid numerical answers: 390,570,750.
19.3 valid numerical answers: 51576,75156,75576.

Numeric input rules:
- no units in answer field;
- decimal comma is canonical; decimal dot may normalize to comma where numeric decimal input is allowed;
- leading plus is unnecessary;
- spaces/units/percent signs are not part of the exam answer;
- signed values must be preserved where mathematically possible (notably tasks 3,16,17);
- scorer compares normalized numeric value for ordinary numeric examples, never by substring.

Result screen:
For every task show task number, student's answer, official answer for assigned official example, and 1/1 or 0/1.
For set/select controls, student's answer is rendered as the assembled exam code, while scorer follows the control semantics above.

Technical/UI reference: ege-matematika-baza-demoversiya-2026 only.
Do not import any 2026 content into 2024.
