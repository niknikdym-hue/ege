PEIS-TRUSTED-HOST-001 VALIDATION
Date: 2026-08-20
Result: PASS
GitHub Actions run: 32387373769
Job: 96485046831
Validated head before durable-result commits: a10dbed2376f246062b7c2121201621e00d888bc

WHAT WAS PROVEN

1. A fresh-main reuse audit found no reusable repository-defined production auth/account/backend trusted host.
2. Shared PeisPersistenceStore identity linking and the existing service HostIdentity contract are reused.
3. The validator signing secret is generated/injected at runtime and is absent from source.
4. Browser-supplied learner_profile_id, identity_refs, anonymous/user identity refs and email are rejected.
5. Anonymous identity ref and learner_profile_id are generated by the trusted host boundary, not browser JavaScript.
6. Anonymous identity is linked through shared persistence.
7. Browser-facing signed token contains only v/kid/opaque identity_ref/iat/exp.
8. Browser-facing token contains no learner_profile_id or email.
9. Cookie projection requires HttpOnly, Secure, SameSite=Lax, Path=/ and bounded Max-Age.
10. Cookie does not expose learner_profile_id or email.
11. Repeated verified-token resolution returns the same learner.
12. Resulting HostIdentity satisfies the existing PEIS service bridge contract.
13. Trusted HostIdentity drives the existing PEIS service bridge and records current-product-shaped Russian evidence.
14. Evidence exists on the anonymous learner before account linking.
15. Later user identity links to the SAME learner_profile_id through shared persistence.
16. Anonymous and user identity refs resolve to the same learner.
17. Account linking does not rewrite, duplicate or migrate the existing EvidenceEvent.
18. The same learner_profile_id is reusable in Russian and Mathematics contexts.
19. Shared persistence rejects reassignment of an identity_ref to another learner and preserves the original link.
20. Email-like user identity ref is rejected.
21. Missing/malformed/tampered/expired/future-issued/unknown-key/wrong-secret tokens are rejected.
22. A signed token containing forbidden learner_profile_id claim is rejected even with a valid signature.

ARCHITECTURAL INVARIANTS

- one cross-subject learner_profile_id
- browser does not choose learner identity
- browser token does not expose learner_profile_id
- server-side identity resolution through shared persistence
- anonymous→account continuity without evidence rewrite
- no Russian-specific learner profile
- no parallel account/auth system declared production
- no production secret committed
- no Tilda/runtime/scoring/localStorage changes

STATUS
REFERENCE_TRUSTED_HOST_IDENTITY_VALIDATED_NOT_PRODUCTION_AUTH

NEXT GATE
PEIS-DEPLOYMENT-SECURITY-001 must decide the real trusted runtime/deployment/security envelope before any public browser traffic is allowed to use this identity boundary. That decision must distinguish reference HMAC/cookie mechanics from production authentication, secret storage, persistence, TLS/origin policy, rate limits, observability, backup/recovery and rollout controls.
