<script>
(function(){
"use strict";
var ROOT_ID="er-trainer",METRICA_ID=110348386,TRAINER_ID="ege-russkiy-trenazher",PROGRESS_KEY="eksamio:ege-russian-trainer:progress:v1",SESSION_KEY="eksamio:ege-russian-trainer:session:v1";
function esc(v){return String(v==null?"":v).replace(/[&<>"']/g,function(c){return{"&":"&amp;","<":"&lt;",">":["&gt;"][0],'"':"&quot;","'":"&#39;"}[c];});}
function wordForm(n,one,few,many){var n10=Math.abs(n)%10,n100=Math.abs(n)%100;return n10===1&&n100!==11?one:n10>=2&&n10<=4&&(n100<12||n100>14)?few:many;}
function numberPhrase(n){return n+" "+wordForm(n,"номер","номера","номеров");}
function selectedNumberPhrase(n){return n+" "+wordForm(n,"выбранный номер","выбранных номера","выбранных номеров");}
function taskPhrase(n){return n+" "+wordForm(n,"задание","задания","заданий");}
function examplePhrase(n){return n+" "+wordForm(n,"пример","примера","примеров");}
function checkedExamplePhrase(n){return n+" "+wordForm(n,"проверенный пример","проверенных примера","проверенных примеров");}
function pointPhrase(n){return n+" "+wordForm(n,"балл","балла","баллов");}
function safeParse(v,fallback){try{return JSON.parse(v);}catch(e){return fallback;}}
function readStore(key,fallback){try{var raw=localStorage.getItem(key);return raw?safeParse(raw,fallback):fallback;}catch(e){return fallback;}}
function writeStore(key,value){try{localStorage.setItem(key,JSON.stringify(value));return true;}catch(e){return false;}}
function removeStore(key){try{localStorage.removeItem(key);}catch(e){}}
function metricGoal(name,params){if(!name)return;var payload=Object.assign({trainerId:TRAINER_ID},params||{});if(typeof window.examPlatformGoal==="function"){window.examPlatformGoal(name,payload);return;}if(typeof window.ym==="function")window.ym(Number(window.EXAM_PLATFORM_METRICA_ID||METRICA_ID),"reachGoal",name,payload);}
function unique(a){return a.filter(function(v,i,x){return x.indexOf(v)===i;});}
function shuffled(a){var x=a.slice();for(var i=x.length-1;i>0;i--){var j=Math.floor(Math.random()*(i+1)),t=x[i];x[i]=x[j];x[j]=t;}return x;}
function normText(v){return String(v==null?"":v).trim().toLowerCase().replace(/ё/g,"е");}
function normCompact(v){return normText(v).replace(/\s+/g,"");}
function displayAnswer(v){return Array.isArray(v)?v.filter(Boolean).join(""):String(v==null?"":v).trim();}
function sameSet(a,b){var x=unique((a||[]).map(String)).sort(),y=unique((b||[]).map(String)).sort();return x.length===(a||[]).length&&y.length===(b||[]).length&&x.join("|")===y.join("|");}
function selectionDiagnostic(card,checked){if(card.kind!=="unordered_digits")return"";var expected=unique((card.answerTokens?card.answerTokens:String(card.answer||"").split("")).map(String)),got=unique((Array.isArray(checked.answer)?checked.answer:[]).map(String)),found=got.filter(function(x){return expected.indexOf(x)>=0;}).length,extra=got.filter(function(x){return expected.indexOf(x)<0;}).length;return'<p><strong>Найдено верных позиций: '+found+' из '+expected.length+'</strong> · лишних позиций: '+extra+'.</p>';}
function scoreCard(card,value){
 if(card.kind==="essay")return null;
 if(card.kind==="ordered_sequence"){
  var got=Array.isArray(value)?value.map(String):[],exp=String(card.answer||"").split("");if(got.length!==exp.length||got.some(function(x){return !x;}))return 0;
  var misses=exp.reduce(function(n,x,i){return n+(x===got[i]?0:1);},0);if(!misses)return Number(card.maxScore)||1;return Number(card.maxScore)===2&&misses<=2?1:0;
 }
 if(card.kind==="unordered_digits"){
  var expected=card.answerTokens?card.answerTokens.map(String):String(card.answer||"").split("");return sameSet(Array.isArray(value)?value:[],expected)?Number(card.maxScore)||1:0;
 }
 var normalize=card.kind==="word_compact"?normCompact:normText,accepted=[card.answer].concat(card.altAnswers||[]).map(normalize);return accepted.indexOf(normalize(value))>=0?Number(card.maxScore)||1:0;
}
function isFilled(card,value){if(card.kind==="essay")return String(value||"").trim().length>=20;if(card.kind==="ordered_sequence")return Array.isArray(value)&&value.length===String(card.answer||"").length&&value.every(Boolean);if(card.kind==="unordered_digits")return Array.isArray(value)&&value.length>0;return String(value||"").trim().length>0;}
function mergeBank(){var out={cards:[],sources:{}};document.querySelectorAll(".er-bank-chunk").forEach(function(el){var j=safeParse(el.textContent,{});if(Array.isArray(j.cards))out.cards=out.cards.concat(j.cards);Object.assign(out.sources,j.sources||{});});return out;}
function init(){
 var root=document.getElementById(ROOT_ID);if(!root)return;
 var meta=safeParse((document.getElementById("er-trainer-meta")||{}).textContent||"{}",{}),bank=mergeBank(),cards=bank.cards||[],sources=bank.sources||{};
 if(meta.tasks&&meta.tasks["4"]){meta.tasks["4"].steps=["Прочитайте каждое слово, поставив ударение на выделенную букву.","Вспомните нормативное ударение и учтите указанную форму слова.","Отметьте все варианты, в которых выделенная буква совпадает с нормативным ударением."];meta.tasks["4"].trap="Опираться на привычное разговорное произношение вместо орфоэпической нормы.";}
 var cardById={};cards.forEach(function(c){cardById[c.id]=c;});
 var progress=readStore(PROGRESS_KEY,{}),session=readStore(SESSION_KEY,null),timerHandle=null,selectedTasks={};for(var n=1;n<=26;n++)selectedTasks[n]=true;selectedTasks[27]=false;
 var setup=root.querySelector('[data-view="setup"]'),sessionView=root.querySelector('[data-view="session"]'),resultView=root.querySelector('[data-view="result"]'),stage=root.querySelector('[data-role="card-stage"]');
 function q(sel,base){return(base||root).querySelector(sel);}function qa(sel,base){return Array.prototype.slice.call((base||root).querySelectorAll(sel));}
 function taskMeta(n){return meta.tasks[String(n)]||{};}function sectionFor(n){return(meta.sections||[]).find(function(s){return s.tasks.indexOf(Number(n))>=0;})||{id:"other",title:"Другое",tasks:[]};}
 function getProgress(id){var p=progress[id];return p&&typeof p==="object"?p:{attempts:0,correct:0,wrong:0,lastCorrect:null,needsReview:false,marked:false,lastAt:0};}
 function saveProgress(){writeStore(PROGRESS_KEY,progress);updateHero();}
 function saveSession(){if(session)writeStore(SESSION_KEY,session);}
 function bankCountForTask(n){return cards.filter(function(c){return c.task===Number(n);}).length;}
 function buildSelectors(){
  var sg=q('[data-role="section-grid"]'),tg=q('[data-role="task-grid"]');sg.innerHTML="";tg.innerHTML="";
  (meta.sections||[]).forEach(function(s){var label=document.createElement("label");label.className="er-section-choice";label.innerHTML='<input type="checkbox" data-section="'+esc(s.id)+'" checked><span><b>'+esc(s.title)+'</b><small>'+numberPhrase(s.tasks.length)+'</small></span>';sg.appendChild(label);});
  for(var n=1;n<=27;n++){var label=document.createElement("label");label.className="er-task";label.setAttribute("data-task",n);label.title="Задание "+n+". "+taskMeta(n).title+" · "+examplePhrase(bankCountForTask(n));label.innerHTML='<input type="checkbox" value="'+n+'"'+(selectedTasks[n]?' checked':'')+'><span>'+n+'</span>';tg.appendChild(label);}
  qa('[data-section]').forEach(function(box){box.addEventListener("change",function(){var s=(meta.sections||[]).find(function(x){return x.id===box.getAttribute("data-section");});(s?s.tasks:[]).forEach(function(n){selectedTasks[n]=box.checked;var input=q('.er-task[data-task="'+n+'"] input');if(input)input.checked=box.checked;});syncSectionChecks();updateRecipe();});});
  qa('.er-task input').forEach(function(box){box.addEventListener("change",function(){selectedTasks[Number(box.value)]=box.checked;syncSectionChecks();updateRecipe();});});syncSectionChecks();
 }
 function syncSectionChecks(){qa('[data-section]').forEach(function(box){var s=(meta.sections||[]).find(function(x){return x.id===box.getAttribute("data-section");}),states=(s?s.tasks:[]).map(function(n){return!!selectedTasks[n];}),on=states.filter(Boolean).length;box.checked=on===states.length;box.indeterminate=on>0&&on<states.length;});}
 function selectedNumbers(){return Object.keys(selectedTasks).filter(function(n){return selectedTasks[n];}).map(Number).sort(function(a,b){return a-b;});}
 function config(){return{mode:(q('input[name="er-mode"]:checked')||{}).value||"practice",status:(q('input[name="er-status"]:checked')||{}).value||"all",count:Number(q("#er-count").value)||10,order:q("#er-order").value||"balanced",tasks:selectedNumbers()};}
 function estimateMinutes(c){if(!c.tasks.length)return 0;var avg=c.tasks.reduce(function(s,n){return s+(Number(taskMeta(n).minutes)||3);},0)/c.tasks.length;return Math.max(2,Math.round(avg*c.count));}
 function updateRecipe(){var c=config(),minutes=estimateMinutes(c),mode=c.mode==="practice"?"Учебный режим":"Контрольный режим",scope=c.tasks.length===26&&c.tasks.indexOf(27)<0?"вся первая часть":selectedNumberPhrase(c.tasks.length),status={all:"все доступные примеры",new:"только новые",errors:"другие примеры по ошибочным навыкам",marked:"только отмеченные"}[c.status];q('[data-role="recipe-title"]').textContent=c.count+" заданий · около "+minutes+" минут";q('[data-role="recipe-copy"]').textContent=mode+" · "+scope+" · "+status+".";q('[data-action="start"]').disabled=!c.tasks.length;}
 function updateHero(){var attempted=0,correct=0,wrong=0,mastered=0;Object.keys(progress).forEach(function(id){var p=getProgress(id);attempted+=p.attempts||0;correct+=p.correct||0;wrong+=p.wrong||0;});for(var n=1;n<=26;n++){var taskCards=cards.filter(function(c){return c.task===n;}),wins=taskCards.reduce(function(s,c){return s+(getProgress(c.id).correct||0);},0);if(wins>=2)mastered++;}q('[data-role="bank-size"]').textContent=checkedExamplePhrase(cards.length);q('[data-role="mastered-count"]').textContent=mastered;q('[data-role="mastered-bar"]').style.width=Math.round(mastered/26*100)+"%";q('[data-role="progress-summary"]').textContent=attempted?"Решено "+attempted+" · верно "+correct+" · ошибок "+wrong+".":"Начните первую тренировку — результаты сохранятся автоматически.";}
 function updateResume(){var box=q('[data-role="resume"]');if(!session||!Array.isArray(session.ids)||!session.ids.length){box.hidden=true;return;}box.hidden=false;var done=Object.keys(session.checked||{}).length,kind=session.status==="finished"?"Тренировка завершена":"Выполнено "+done+" из "+session.ids.length;q('[data-role="resume-copy"]').textContent=kind+" · "+(session.mode==="exam"?"контрольный":"учебный")+" режим.";q('[data-action="resume-session"]').textContent=session.status==="finished"?"Посмотреть результат":"Продолжить";}
 function setGoal(goal){qa('[data-goal]').forEach(function(b){b.setAttribute("aria-pressed",b.getAttribute("data-goal")===goal?"true":"false");});var values={warmup:{count:"5",mode:"practice",status:"all"},practice:{count:"10",mode:"practice",status:"all"},control:{count:"20",mode:"exam",status:"all"},mistakes:{count:"10",mode:"practice",status:"errors"}}[goal];if(!values)return;q("#er-count").value=values.count;var m=q('input[name="er-mode"][value="'+values.mode+'"]'),s=q('input[name="er-status"][value="'+values.status+'"]');if(m)m.checked=true;if(s)s.checked=true;updateRecipe();}
 function filteredCards(c){function allowed(card){return c.tasks.indexOf(card.task)>=0&&!(c.mode==="exam"&&card.legacyFormat);}if(c.status==="errors"){var errorCards=cards.filter(function(card){var p=getProgress(card.id);return allowed(card)&&(p.needsReview||p.lastCorrect===false);}),errorTasks=unique(errorCards.map(function(card){return card.task;})),alternatives=cards.filter(function(card){var p=getProgress(card.id);return allowed(card)&&errorTasks.indexOf(card.task)>=0&&!p.needsReview&&p.lastCorrect!==false;});return alternatives.length?alternatives:errorCards;}return cards.filter(function(card){if(!allowed(card))return false;var p=getProgress(card.id);if(c.status==="new")return!p.attempts;if(c.status==="marked")return!!p.marked;return true;});}
 function cardRank(card){var p=getProgress(card.id);return(p.attempts||0)*10000000000000+(p.lastAt||0)+Math.random()*1000;}
 function pickSessionCards(c){
  var eligible=filteredCards(c);if(!eligible.length)return[];var wanted=Math.min(c.count,eligible.length),picked=[],used={};
  if(c.order==="mixed"){return shuffled(eligible).sort(function(a,b){return cardRank(a)-cardRank(b);}).slice(0,wanted);}
  var groups={};eligible.forEach(function(card){(groups[card.task]||(groups[card.task]=[])).push(card);});Object.keys(groups).forEach(function(n){groups[n].sort(function(a,b){return cardRank(a)-cardRank(b);});});
  var tasks=Object.keys(groups).map(Number);if(c.order==="exam")tasks.sort(function(a,b){return a-b;});else tasks=shuffled(tasks).sort(function(a,b){var aa=groups[a].reduce(function(s,x){return s+getProgress(x.id).attempts;},0),bb=groups[b].reduce(function(s,x){return s+getProgress(x.id).attempts;},0);return aa-bb;});
  while(picked.length<wanted){var added=false;for(var i=0;i<tasks.length&&picked.length<wanted;i++){var list=groups[tasks[i]],candidate=list.find(function(x){return!used[x.id];});if(candidate){picked.push(candidate);used[candidate.id]=true;added=true;}}if(!added)break;}
  if(c.order==="exam")picked.sort(function(a,b){return a.task-b.task;});else if(c.order==="mixed")picked=shuffled(picked);return picked;
 }
 function startWithCards(chosen,c){
  var now=Date.now(),minutes=chosen.reduce(function(s,card){return s+(Number(taskMeta(card.task).minutes)||3);},0);session={version:1,status:"running",mode:c.mode,ids:chosen.map(function(x){return x.id;}),current:0,answers:{},checked:{},recorded:{},startedAt:now,endsAt:c.mode==="exam"?now+Math.max(2,minutes)*60000:null,completedAt:null,config:c};saveSession();metricGoal("ep_trainer_start",{mode:c.mode,count:chosen.length,tasks:c.tasks.join(",")});showSession();
 }
 function startNew(){var c=config(),chosen=pickSessionCards(c),alert=q('[data-role="setup-alert"]');if(!c.tasks.length){alert.hidden=false;alert.textContent="Выберите хотя бы один номер задания.";return;}if(!chosen.length){alert.hidden=false;alert.textContent=c.status==="errors"?"В выбранных номерах пока нет ошибок. Начните обычную тренировку или выберите фильтр «Все».":"Для этого фильтра пока нет доступных примеров.";return;}alert.hidden=true;metricGoal("ep_build_training",{mode:c.mode,status:c.status,count:chosen.length,order:c.order,tasks:c.tasks.join(",")});startWithCards(chosen,c);}
 function currentCard(){return session&&cardById[session.ids[session.current]];}
 function showOnly(view){setup.hidden=view!=="setup";sessionView.hidden=view!=="session";resultView.hidden=view!=="result";}
 function showSession(){if(!session||!session.ids.length)return;if(session.status==="finished"){renderResult();return;}showOnly("session");renderMap();renderCard();startTimer();root.scrollIntoView({behavior:"smooth",block:"start"});}
 function renderMap(){var map=q('[data-role="session-map"]');map.innerHTML="";session.ids.forEach(function(id,i){var card=cardById[id],checked=session.checked[id],b=document.createElement("button");b.type="button";b.textContent=card?card.task:i+1;b.title="Позиция "+(i+1)+": задание "+(card?card.task:"—");b.setAttribute("aria-current",i===session.current?"true":"false");var state=checked?(checked.score===checked.max?"right":checked.score>0?"wrong":"wrong"):(isFilled(card,session.answers[id])?"filled":"empty");b.setAttribute("data-state",state);b.setAttribute("data-marked",getProgress(id).marked?"true":"false");b.addEventListener("click",function(){session.current=i;saveSession();renderMap();renderCard();});map.appendChild(b);});var done=Object.keys(session.checked||{}).length;q('[data-role="session-position"]').textContent="Задание "+(session.current+1)+" из "+session.ids.length;q('[data-role="session-mode"]').textContent=session.mode==="exam"?"Контрольный режим":"Учебный режим";q('[data-role="session-progress"]').style.width=Math.round((session.mode==="exam"?session.current:done)/session.ids.length*100)+"%";}
 function answerControl(card,value,locked){var disabled=locked?' disabled':'';
  if(card.kind==="essay")return'<div class="er-answer"><label class="er-answer-label" for="er-answer-input">Текст сочинения</label><textarea class="er-textarea" id="er-answer-input"'+disabled+'>'+esc(value||"")+'</textarea><p class="er-answer-help">Текст сохраняется автоматически. Тренажёр не выдаёт автоматическую экспертную оценку сочинения.</p></div>';
  if(card.kind==="unordered_digits"){var selected=Array.isArray(value)?value.map(String):[];return'<div class="er-answer"><span class="er-answer-label">Выберите все верные позиции</span><div class="er-choice-grid">'+(card.options||[]).map(function(x){x=String(x);return'<label class="er-choice-box"><input type="checkbox" data-answer-choice value="'+esc(x)+'"'+(selected.indexOf(x)>=0?' checked':'')+disabled+'><span>'+esc(x)+'</span></label>';}).join("")+'</div><p class="er-answer-help">Порядок выбора не влияет на результат. Лишняя или пропущенная позиция делает ответ неверным.</p></div>';
  }
  if(card.kind==="ordered_sequence"){var arr=Array.isArray(value)?value:[],letters="АБВГДЕЖЗ".split("");return'<div class="er-answer"><span class="er-answer-label">Заполните соответствие по позициям</span><div class="er-match-grid">'+String(card.answer||"").split("").map(function(_,i){return'<div class="er-match-item"><label for="er-match-'+i+'">'+letters[i]+'</label><select id="er-match-'+i+'" data-answer-match="'+i+'"'+disabled+'><option value="">—</option>'+(card.options||[]).map(function(x){return'<option value="'+esc(x)+'"'+(String(arr[i]||"")===String(x)?' selected':'')+'>'+esc(x)+'</option>';}).join("")+'</select></div>';}).join("")+'</div><p class="er-answer-help">Порядок имеет значение: каждая цифра относится к своей букве.</p></div>';
  }
  return'<div class="er-answer"><label class="er-answer-label" for="er-answer-input">Ответ</label><input class="er-input" id="er-answer-input" type="text" autocomplete="off" spellcheck="false" value="'+esc(value||"")+'"'+disabled+'><p class="er-answer-help">Введите только требуемое слово или выражение.</p></div>';
 }
 function correctDisplay(card){if(card.kind==="essay")return"Самопроверка по критериям";var a=[card.answer].concat(card.altAnswers||[]).map(function(x){return String(x);});return unique(a).join("; ");}
 function feedbackHtml(card,checked){if(!checked)return"";if(card.kind==="essay")return'<section class="er-feedback" data-result="partial"><h3>Переходим к самопроверке</h3><p>Сочинение сохранено. Проверьте наличие позиции автора, двух примеров с пояснениями, смысловой связи, собственного отношения и обоснования.</p>'+explanationGrid(card)+'</section>';var result=checked.score===checked.max?"right":checked.score>0?"partial":"wrong",title=result==="right"?"Верно":result==="partial"?"Частично верно":"Нужно повторить";return'<section class="er-feedback" data-result="'+result+'"><h3>'+title+' · '+pointPhrase(checked.score)+' из '+checked.max+'</h3>'+selectionDiagnostic(card,checked)+'<p>Ваш ответ: <strong>'+esc(displayAnswer(checked.answer)||"—")+'</strong></p><p>Правильный ответ: <strong>'+esc(correctDisplay(card))+'</strong></p>'+(card.explanation?'<p>'+esc(card.explanation)+'</p>':'')+explanationGrid(card)+'</section>';}
 function explanationGrid(card){var m=taskMeta(card.task);return'<div class="er-explain-grid"><div class="er-explain-box"><strong>Алгоритм</strong><ol>'+(m.steps||[]).map(function(x){return'<li>'+esc(x)+'</li>';}).join("")+'</ol></div><div class="er-explain-box"><strong>Типичная ловушка</strong><p>'+esc(m.trap||"")+'</p></div></div>';}
 function separatePromptMaterial(card){if(card.task===27)return;var prompt=q(".er-prompt",stage);if(!prompt)return;var children=Array.prototype.slice.call(prompt.children);if(children.length<2||children[0].tagName!=="P"||children[1].tagName!=="P")return;children[0].classList.add("er-task-instruction");var material=document.createElement("div"),label=document.createElement("span");material.className="er-task-material";material.setAttribute("aria-label","Материал задания");label.className="er-task-material-label";label.textContent="Материал задания";material.appendChild(label);children.slice(1).forEach(function(node){material.appendChild(node);});prompt.appendChild(material);}
 function renderCard(){var card=currentCard();if(!card){finishSession(true);return;}var m=taskMeta(card.task),section=sectionFor(card.task),checked=session.checked[card.id],locked=!!checked&&session.mode==="practice",source=card.sourceKey?sources[card.sourceKey]:"";stage.innerHTML='<header class="er-card-head"><div><div class="er-card-number">Задание '+card.task+' · '+esc(section.title)+'</div><h2 class="er-card-title">'+esc(m.title||card.title||"")+'</h2><p class="er-sub">'+esc(m.skill||"")+'</p></div><div class="er-card-tags"><span class="er-pill">Пример '+esc(card.bankIndex)+' из '+esc(card.bankTotal)+'</span>'+(card.legacyFormat?'<span class="er-pill">Формат прошлых лет</span>':'')+(getProgress(card.id).marked?'<span class="er-pill">★ Отмечено</span>':'')+'</div></header>'+(source?'<section class="er-source"><p class="er-source-title">Исходный текст</p>'+source+'</section>':'')+'<div class="er-prompt">'+card.promptHtml+'</div>'+answerControl(card,session.answers[card.id],locked)+feedbackHtml(card,checked)+'<p class="er-alert" data-role="answer-alert" hidden></p><div class="er-card-actions"><div class="er-action-group"><button class="er-button er-button--secondary er-button--small" type="button" data-card-action="prev"'+(session.current===0?' disabled':'')+'>← Предыдущее</button>'+(session.mode==="practice"&&!checked?'<button class="er-button er-button--small" type="button" data-card-action="check">Проверить ответ</button>':'<button class="er-button er-button--small" type="button" data-card-action="next">'+(session.current===session.ids.length-1?'Завершить':'Следующее →')+'</button>')+'</div><div class="er-action-group"><button class="er-button er-button--secondary er-button--small" type="button" data-card-action="mark">'+(getProgress(card.id).marked?'Снять отметку':'Отметить')+'</button>'+(session.mode==="exam"?'<button class="er-button er-button--ghost er-button--small" type="button" data-card-action="finish">Завершить досрочно</button>':'')+'</div></div>';
  separatePromptMaterial(card);bindAnswer(card,locked);
  q('[data-card-action="prev"]',stage).addEventListener("click",function(){session.current=Math.max(0,session.current-1);saveSession();renderMap();renderCard();});var check=q('[data-card-action="check"]',stage);if(check)check.addEventListener("click",function(){checkCurrent(card);});var next=q('[data-card-action="next"]',stage);if(next)next.addEventListener("click",function(){if(session.current>=session.ids.length-1)finishSession(false);else{session.current++;saveSession();renderMap();renderCard();}});q('[data-card-action="mark"]',stage).addEventListener("click",function(){var p=getProgress(card.id);p.marked=!p.marked;progress[card.id]=p;saveProgress();renderMap();renderCard();});var finish=q('[data-card-action="finish"]',stage);if(finish)finish.addEventListener("click",function(){finishSession(false);});
 }
 function bindAnswer(card,locked){if(locked)return;function persist(value){session.answers[card.id]=value;saveSession();renderMap();}
  if(card.kind==="unordered_digits"){qa('[data-answer-choice]',stage).forEach(function(x){x.addEventListener("change",function(){persist(qa('[data-answer-choice]:checked',stage).map(function(y){return y.value;}).sort(function(a,b){return Number(a)-Number(b);}));});});return;}
  if(card.kind==="ordered_sequence"){qa('[data-answer-match]',stage).forEach(function(x){x.addEventListener("change",function(){var arr=Array.isArray(session.answers[card.id])?session.answers[card.id].slice():Array(String(card.answer||"").length).fill("");arr[Number(x.getAttribute("data-answer-match"))]=x.value;persist(arr);});});return;}
  var input=q("#er-answer-input",stage);if(input)input.addEventListener("input",function(){persist(input.value);});
 }
 function recordCard(card,checked){if(session.recorded[card.id])return;if(card.kind==="essay"){session.recorded[card.id]=true;return;}var p=getProgress(card.id);p.attempts=(p.attempts||0)+1;p.lastAt=Date.now();if(checked.score===checked.max){p.correct=(p.correct||0)+1;p.lastCorrect=true;p.needsReview=false;}else{p.wrong=(p.wrong||0)+1;p.lastCorrect=false;p.needsReview=true;}progress[card.id]=p;session.recorded[card.id]=true;saveProgress();}
 function checkCurrent(card){var value=session.answers[card.id],alert=q('[data-role="answer-alert"]',stage);if(!isFilled(card,value)){alert.hidden=false;alert.textContent=card.kind==="essay"?"Добавьте текст сочинения перед самопроверкой.":"Сначала заполните ответ.";alert.scrollIntoView({behavior:"smooth",block:"center"});return;}var score=scoreCard(card,value),checked={score:score,max:card.kind==="essay"?null:Number(card.maxScore)||1,answer:value};session.checked[card.id]=checked;recordCard(card,checked);saveSession();if(card.kind!=="essay")metricGoal(score===checked.max?"ep_trainer_answer_correct":"ep_trainer_answer_wrong",{mode:session.mode,task:card.task,cardId:card.id,score:score,max:checked.max});renderMap();renderCard();}
 function startTimer(){clearInterval(timerHandle);function tick(){if(!session||session.status!=="running")return;var ms=session.mode==="exam"?Math.max(0,(session.endsAt||0)-Date.now()):Math.max(0,Date.now()-session.startedAt),total=Math.floor(ms/1000),h=Math.floor(total/3600),m=Math.floor(total%3600/60),s=total%60;q('[data-role="timer"]').textContent=(h?String(h).padStart(2,"0")+":":"")+String(m).padStart(2,"0")+":"+String(s).padStart(2,"0");if(session.mode==="exam"&&ms<=0){clearInterval(timerHandle);finishSession(true);}}tick();timerHandle=setInterval(tick,1000);}
 function finishSession(auto){if(!session||session.status==="finished")return;if(!auto&&!window.confirm("Завершить тренировку и перейти к результатам?"))return;clearInterval(timerHandle);session.ids.forEach(function(id){var card=cardById[id];if(!session.checked[id]){var value=session.answers[id],score=scoreCard(card,value);session.checked[id]={score:score,max:card.kind==="essay"?null:Number(card.maxScore)||1,answer:value};}recordCard(card,session.checked[id]);});session.status="finished";session.completedAt=Date.now();var completedStats=resultStats();if(!session.metricCompleteSent){metricGoal("ep_trainer_complete",{mode:session.mode,count:session.ids.length,score:completedStats.score,max:completedStats.max,correct:completedStats.correct,wrong:completedStats.wrong});session.metricCompleteSent=true;}saveSession();renderResult();}
 function resultStats(){var out={score:0,max:0,correct:0,wrong:0,essay:0,bySection:{},wrongCards:[]};session.ids.forEach(function(id){var card=cardById[id],ch=session.checked[id],sec=sectionFor(card.task);if(card.kind==="essay"){out.essay++;return;}out.score+=Number(ch.score)||0;out.max+=Number(ch.max)||0;var ok=ch.score===ch.max;if(ok)out.correct++;else{out.wrong++;out.wrongCards.push(card);}var row=out.bySection[sec.id]||(out.bySection[sec.id]={title:sec.title,score:0,max:0,errors:0});row.score+=Number(ch.score)||0;row.max+=Number(ch.max)||0;if(!ok)row.errors++;});return out;}
 function renderResult(){if(!session)return;showOnly("result");clearInterval(timerHandle);var stats=resultStats(),percent=stats.max?Math.round(stats.score/stats.max*100):0;if(!session.metricResultSent){metricGoal("ep_result_view",{mode:session.mode,count:session.ids.length,score:stats.score,max:stats.max,percent:percent});session.metricResultSent=true;saveSession();}q('[data-role="result-score"]').textContent=stats.max?stats.score+"/"+stats.max:"—";q('[data-role="result-correct"]').textContent=stats.correct;q('[data-role="result-wrong"]').textContent=stats.wrong;q('[data-role="result-summary"]').textContent="Выполнено "+taskPhrase(session.ids.length)+(stats.max?" · точность "+percent+"%. ":". Автоматически проверяемых заданий нет. ")+(stats.essay?"Сочинение сохранено для самостоятельной или экспертной проверки.":"");
  var breakdown=q('[data-role="breakdown"]');breakdown.innerHTML=Object.keys(stats.bySection).map(function(id){var x=stats.bySection[id],p=x.max?Math.round(x.score/x.max*100):0;return'<div class="er-breakdown-item"><div><strong>'+esc(x.title)+'</strong><small>'+(x.errors?"Ошибок: "+x.errors:"Без ошибок")+'</small></div><b>'+p+'%</b></div>';}).join("")||'<p class="er-sub">Автоматически проверяемых заданий не было.</p>';
  var taskErrors={};stats.wrongCards.forEach(function(c){taskErrors[c.task]=(taskErrors[c.task]||0)+1;});var ranked=Object.keys(taskErrors).map(Number).sort(function(a,b){return taskErrors[b]-taskErrors[a];}).slice(0,4),rec=q('[data-role="recommendations"]');rec.innerHTML=ranked.length?'<ol>'+ranked.map(function(n){return'<li><strong>Задание '+n+'. '+esc(taskMeta(n).title)+'</strong><br><span class="er-small">'+esc(taskMeta(n).skill)+'</span></li>';}).join("")+'</ol><p class="er-small">В повторе система предложит другие примеры на эти навыки.</p>':'<p>Ошибок нет. Для закрепления выберите новые примеры или контрольный режим.</p>';
  var review=q('[data-role="review-list"]');review.innerHTML=session.ids.map(function(id){var card=cardById[id],ch=session.checked[id],ok=card.kind==="essay"?true:ch.score===ch.max;return'<article class="er-review-item" data-correct="'+(ok?"true":"false")+'"><strong>Задание '+card.task+' · '+esc(taskMeta(card.task).title)+'</strong>'+(card.kind==="essay"?'<p>Сочинение сохранено. Требуется самостоятельная или экспертная оценка.</p>':'<p>Ваш ответ: <b>'+esc(displayAnswer(ch.answer)||"—")+'</b> · правильный: <b>'+esc(correctDisplay(card))+'</b></p><p>'+esc(card.explanation||"Повторите алгоритм и типичную ловушку для этого номера.")+'</p>')+'</article>';}).join("");q('[data-action="repeat-errors"]').disabled=!stats.wrongCards.length;root.scrollIntoView({behavior:"smooth",block:"start"});updateResume();}
 function repeatErrors(){var stats=resultStats(),byTask=unique(stats.wrongCards.map(function(c){return c.task;})),used=session.ids.slice(),chosen=[];byTask.forEach(function(n){var candidates=cards.filter(function(c){return c.task===n&&used.indexOf(c.id)<0;}).sort(function(a,b){return cardRank(a)-cardRank(b);});if(!candidates.length)candidates=cards.filter(function(c){return c.task===n;}).sort(function(a,b){return cardRank(a)-cardRank(b);});if(candidates[0])chosen.push(candidates[0]);});if(!chosen.length)return;metricGoal("ep_repeat_errors",{count:chosen.length,tasks:byTask.join(",")});startWithCards(chosen,{mode:"practice",status:"errors",count:chosen.length,order:"balanced",tasks:byTask});}
 function copyReport(){var s=resultStats(),lines=["Эксамио · тренажёр ЕГЭ по русскому языку","Результат: "+s.score+" из "+s.max,"Верно: "+s.correct,"Нужно повторить: "+s.wrong];Object.keys(s.bySection).forEach(function(id){var x=s.bySection[id];lines.push(x.title+": "+x.score+" из "+x.max);});var text=lines.join("\n");function copied(){q('[data-action="copy-report"]').textContent="Отчёт скопирован";metricGoal("ep_report_copy",{score:s.score,max:s.max});}if(navigator.clipboard&&navigator.clipboard.writeText)navigator.clipboard.writeText(text).then(copied).catch(function(){window.prompt("Скопируйте отчёт",text);copied();});else{window.prompt("Скопируйте отчёт",text);copied();}}
 function goSetup(){clearInterval(timerHandle);session=null;removeStore(SESSION_KEY);showOnly("setup");updateResume();root.scrollIntoView({behavior:"smooth",block:"start"});}
 qa('[data-goal]').forEach(function(b){b.addEventListener("click",function(){setGoal(b.getAttribute("data-goal"));});});qa('input[name="er-mode"],input[name="er-status"],#er-count,#er-order').forEach(function(x){x.addEventListener("change",updateRecipe);});q('[data-action="select-short"]').addEventListener("click",function(){for(var n=1;n<=27;n++)selectedTasks[n]=n<=26;buildSelectors();updateRecipe();});q('[data-action="select-all"]').addEventListener("click",function(){for(var n=1;n<=27;n++)selectedTasks[n]=true;buildSelectors();updateRecipe();});q('[data-action="clear-tasks"]').addEventListener("click",function(){for(var n=1;n<=27;n++)selectedTasks[n]=false;buildSelectors();updateRecipe();});q('[data-action="start"]').addEventListener("click",startNew);q('[data-action="discard-session"]').addEventListener("click",function(){session=null;removeStore(SESSION_KEY);updateResume();});q('[data-action="resume-session"]').addEventListener("click",function(){session.status==="finished"?renderResult():showSession();});q('[data-action="repeat-errors"]').addEventListener("click",repeatErrors);q('[data-action="new-session"]').addEventListener("click",goSetup);q('[data-action="copy-report"]').addEventListener("click",copyReport);q('[data-action="print"]').addEventListener("click",function(){var s=resultStats();metricGoal("ep_report_print",{score:s.score,max:s.max});window.print();});
 buildSelectors();updateHero();updateRecipe();updateResume();
}
window.__erTrainerTest={scoreCard:scoreCard,isFilled:isFilled,normText:normText,normCompact:normCompact,sameSet:sameSet,pointPhrase:pointPhrase,selectionDiagnostic:selectionDiagnostic};
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",init);else init();
})();
</script>
