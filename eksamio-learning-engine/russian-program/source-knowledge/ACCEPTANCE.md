# Acceptance contract — Russian official source knowledge v1.0

Required before merge:

- exactly 4 canonical launch source records and 8 concrete source documents;
- exact SHA-256 fingerprints for every concrete document;
- 1400 normalized requirement records;
- all 16 `RU-PROG` modules have non-zero official requirement accounting;
- deterministic requirement package and executable crosswalk hashes are stable across repeated runs;
- every requirement is classified by the executable crosswalk;
- `FIPI-OGE-RU-2027-PROJECT` cannot enter launch truth;
- missing fingerprint, missing locator, unknown module, false canonicalization, commercial textbook ingestion, long copied prose and a zero-requirements module fail validation;
- `ru-*` identities remain unadmitted;
- PR #139 references remain read-only and pinned to `f16884ec4f8992ee9ad01c2930c42349f579bc70`;
- Russian content preflight remains `BLOCKED_SUBJECT`;
- public traffic, production charges, PEIS network writes and Yandex gateway apply remain OFF;
- no provider execution, deployment, DNS, paid request or demo/scoring change.
